Never run the project at all times unless the user asks you to.
Also do typescript checks to make sure the code works.
Also do not build it unless it really really is needed. In Development the user uses 'npm run dev'